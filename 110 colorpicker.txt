from tkinter import *
from tkinter import colorchooser #cause submodule

def clickk():
	color = colorchooser.askcolor() # return ((floatr, floatg, floatb) hexstring)
	colorhex=color[1]
	window.config(bg=colorhex)
	you can do oneline doesnt matter
	
window = Tk()  
window.geometry("420x420")
button = Button(text="click me",command=click)

window.mainloop()
